# Build artifacts generated by scripts/build-feed.mjs at deploy time.
# Unlike digests.json (which the daily-draft flow needs in-repo), these are
# regenerated on every build and should not be committed.
/feed.xml
/sitemap.xml
